# --- Shopify -----------------------------------------------------------------
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_APP_URL=
# Read-only throughout: the app mirrors the catalog and never writes to the store.
SCOPES=read_products,read_product_listings,read_collection_listings,read_inventory,read_content,write_pixels,read_customer_events

# --- Database ----------------------------------------------------------------
# Runtime uses the pooled connection; migrations and DDL use the direct one.
# On Supabase: DATABASE_URL = Transaction pooler (6543), DIRECT_URL = Direct (5432).
DATABASE_URL="postgresql://postgres:password@127.0.0.1:5432/anotherdev_search?schema=public"
DIRECT_URL="postgresql://postgres:password@127.0.0.1:5432/anotherdev_search?schema=public"

# --- Search backend ----------------------------------------------------------
SEARCH_BACKEND=postgres

# How long facet aggregates are reused. Facets are the expensive half of a
# search, and they do not change between pages or sorts of the same query.
FACET_CACHE_TTL_MS=20000

# --- Semantic search (optional) ----------------------------------------------
# Off by default: keyword search is complete without it. Turning it on adds an
# embeddings API call per new/changed product and per uncached query, and needs
# pgvector in Postgres. Pro plan only.
SEMANTIC_SEARCH_ENABLED=false
EMBEDDINGS_PROVIDER=voyage        # voyage | openai
# VOYAGE_API_KEY=
# OPENAI_API_KEY=
# EMBEDDINGS_MODEL=voyage-3.5-lite

# --- Billing -----------------------------------------------------------------
# Test charges are approved in the admin but never billed. Leave unset to infer
# from NODE_ENV; set explicitly so a deploy that forgets NODE_ENV=production
# cannot silently issue charges that never take a payment.
SHOPIFY_BILLING_TEST=false

# --- Operator access -----------------------------------------------------------
# Comma-separated myshopify domains allowed to set a plan override from the
# Plans page (a paid tier with no Shopify charge). Leave EMPTY in production
# unless you mean it: any shop listed here can grant itself Pro for free.
OPERATOR_SHOPS=

# --- Analytics ---------------------------------------------------------------
# Raw search events kept this long. Pruned opportunistically after each catalog
# sync; a little beyond the 90-day Pro window so an upgrade shows history at once.
ANALYTICS_RETENTION_DAYS=180

# How long the anonymous storefront session token stays on a search event. It is
# only needed to attribute a click or add-to-cart back to its search (a 2-hour
# window), so it is cleared a day later and the retained analytics hold no
# per-shopper identifier at all.
ANALYTICS_SESSION_TOKEN_RETENTION_HOURS=24

# --- Local dev only ----------------------------------------------------------
# SHOP_CUSTOM_DOMAIN=

# ---------------------------------------------------------------------------
# Scheduled catalog reconciliation (app/routes/cron.sync.tsx)
# ---------------------------------------------------------------------------
# Shared secret for the nightly re-sync endpoint. UNSET MEANS THE ENDPOINT
# REFUSES EVERY REQUEST — an unset variable must never mean "open to anyone".
# Call it once a day:
#   curl -X POST https://<app>/cron/sync -H "Authorization: Bearer $CRON_SECRET"
CRON_SECRET=
# How many shops one invocation starts. The next run picks up the rest.
CRON_SYNC_BATCH=5

# The storefront path of the App Proxy, if `[app_proxy] prefix/subpath` in
# shopify.app.toml is ever changed from the default. Shared by the storefront
# links, the admin's "view this page" links and the Web Pixel.
APP_PROXY_BASE=/apps/anotherdev-search

# Search by photo. Needs SEMANTIC_SEARCH_ENABLED=true, the Voyage provider and a
# VOYAGE_API_KEY: it embeds the shopper's picture into the same vector space as
# the products, which OpenAI has no embedding endpoint for.
#
# Turning this on switches product embeddings to a multimodal model, which
# INVALIDATES every embedding already stored — re-run a sync afterwards so the
# backfill rebuilds them.
EMBEDDINGS_MULTIMODAL=false
